IndentationError: expected an indented block
Python expects at least one indented statement after a ':' because the ':' statement begins a block. 
Without indentation, Python doesn't know where one block begins (or rather where it ends), so it throws an indentation error.
